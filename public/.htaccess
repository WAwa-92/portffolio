Options -Indexes

RewriteEngine On

# Laisser passer les fichiers et dossiers réels (CSS, JS, images...)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Tout le reste → index.php
RewriteRule ^ index.php [QSA,L]
